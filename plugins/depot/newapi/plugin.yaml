name: depot-newapi
slot: depot
default: false
description: >
  External depot, not part of this repo. Same role as “OpenAI” or “Anthropic”
  from DMM’s point of view: a URL + token + quota. Compose their image;
  do not vendor (AGPL).
# Hermes OPENAI_BASE_URL -> http://new-api:3000/v1
interface:
  - openai_compat
